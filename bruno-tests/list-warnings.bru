meta {
  name: List Warnings
  type: http
  seq: 1
}

get {
  url: http://localhost:3000/?state=NSW
  body: none
  auth: none
}

tests {
  test("Status code should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("Response should be an array", function() {
    expect(Array.isArray(res.getBody())).to.be.true;
  });
}
